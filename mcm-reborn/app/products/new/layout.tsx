import type { ReactNode } from "react";

type ProductNewLayoutProps = {
  children: ReactNode;
};

export default function ProductNewLayout({ children }: ProductNewLayoutProps) {
  return children;
}

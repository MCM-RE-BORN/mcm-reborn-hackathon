# 결정 기록

승인된 제품·기술 결정을 append-only로 기록한다. 기존 행을 조용히 고치지 말고 변경 사유와 대체 결정을 새 행으로 추가한다.

| ID | 날짜 | 상태 | 결정 | 근거·영향 |
|---|---|---|---|---|
| DEC-001 | 2026-08-11 | 승인 | `develop`의 2주 해커톤 MVP를 현재 제품 기준으로 삼는다. | 로컬 확장 기획은 Phase 2로 분리하고 MVP 반영은 별도 승인한다. |
| DEC-002 | 2026-08-11 | 승인 | MVP 채널은 웹, 역할은 `CUSTOMER`와 통합 운영자 `OPERATOR`로 제한한다. | 독립 장인·브랜드 관리자와 iOS는 Phase 2다. |
| DEC-003 | 2026-08-11 | 승인 | HTTP API의 기계 판독 기준은 `openapi.yaml`이다. | API 가이드·Mock·DB가 다르면 계약 변경 절차로 조정한다. |
| DEC-004 | 2026-08-11 | 승인 | 분석은 live·fixture·hybrid 모드를 지원하고 결제·물류·ESG·보증서는 Mock으로 시연한다. | 데모 복구성을 확보하되 실제 서비스처럼 과장하지 않는다. |
| DEC-005 | 2026-08-11 | 승인 | 협업 규칙은 `AGENTS.md`, `AI_RULES.md`, `docs/`와 저장소 스킬로 배포한다. | Codex와 GitHub Copilot을 포함한 에이전트가 저장소 안에서 같은 기준을 발견한다. |
| DEC-006 | 2026-08-11 | 승인 | 계약 변경은 별도 이슈와 프런트엔드·백엔드 양쪽 승인을 요구한다. | OpenAPI → Mock·타입 → 서버·DB → 클라이언트 → 테스트·문서 순서를 유지한다. |
| DEC-007 | 2026-08-11 | 승인 | 사진 품질 미달은 `422 IMAGE_QUALITY_INSUFFICIENT`와 재촬영 안내를 반환하고 성공 분석을 만들지 않는다. | 사용자 입력 오류를 제공자 장애 폴백으로 숨기지 않으며 같은 제품의 사진을 다시 제출하게 한다. |
| DEC-008 | 2026-08-11 | 승인 | `REVIEW_REQUIRED`는 정품·가품 판정이 아닌 신청 전 수동 검토 게이트다. | `PENDING` 검토 건은 만들지만 신청은 만들지 않는다. 검토 완료 API·UI는 MVP에서 제외한다. |
| DEC-009 | 2026-08-11 | 승인 | 웹 앱은 `mcm-reborn/`의 Next.js 16.3.0·React 19.2.8 scaffold에서 구현한다. | 현재 scaffold 존재는 MVP 기능 구현 완료를 뜻하지 않으며 실제 동작은 계약과 완료 기준으로 검증한다. |
| DEC-010 | 2026-08-17 | 승인 | `docs/MVP_DEMO_CANONICAL.md`를 현재 데모 MVP의 제품·흐름 SSOT로 삼는다. | 최종 기획과 자연스러운 시연이 기존 2주 계약 경계보다 우선한다. DEC-001의 충돌 범위를 대체한다. |
| DEC-011 | 2026-08-17 | 승인 | 실제 AI가 안정적으로 판단하기 어려운 값은 중앙 Fixture 또는 재현 가능한 예상치로 시연한다. | 예상치임을 표시하고 같은 접수에서 값이 변하지 않게 한다. DEC-004의 fixture 원칙을 구체화한다. |
| DEC-012 | 2026-08-17 | 승인 | 제품 사진은 정면·측면·내부·각인 4슬롯, JPG/JPEG·PNG 최소 3장·최대 4장, 장당 10MB로 통일한다. | 최종 와이어프레임을 채택하며 이전 1~4장·WebP·6MB 결정을 대체한다. |
| DEC-013 | 2026-08-17 | 승인 | AI 사전 판단으로 Mock 주문·결제를 완료하고, 공식 장인 실물 검수는 주문·수거 후 마지막 제작 조건 점검으로 수행한다. | 주문 전 수동 검토 차단·운영자 선승인을 골든 패스에서 제거한다. DEC-008을 대체한다. |
| DEC-014 | 2026-08-17 | 승인 | 실물 검수 변경 조건은 고객 승인 전 제작하지 않으며, 거절·제작 불가는 취소와 Mock 환불로 종료한다. | 최종 유저플로우의 변경 승인·거절 분기를 데모에 포함한다. |
| DEC-015 | 2026-08-17 | 승인 | 접수·주문·보증서 전 화면은 `MCM_BACKPACK_CHANGE_APPROVED_20260817` 중앙 시나리오를 공유한다. | 화면별 제품·ID·가격·재활용률 불일치를 제거하고 변경 이력을 한 주문으로 시연한다. |
| DEC-016 | 2026-08-18 | 승인 | 주문 lifecycle 무결성을 하나의 변경 단위로 보완한다: `COMPLETED` 전 보증서 생성을 DB에서 차단하고, 운영자 lifecycle command는 허용된 인접 전이만 수행하며, `CHANGE_REQUIRED` 검수와 변경안은 원자 처리하고, 기존 DB에는 버전 migration·backfill을 적용하며, canonical 배송 Fixture를 둔다. | 사용자가 후속 구현을 명시 승인했다. API 계약 v2.0.0의 미구현 결손을 완성하므로 현 버전을 유지하되, OpenAPI·Mock·bootstrap·migration·검증기를 같은 변경 단위로 배포하고 부분 롤백하지 않는다. |
| DEC-017 | 2026-08-18 | 승인 | Figma `디자인` 파일의 `최종 디자인` 페이지(`228:391`)를 현재 화면의 시각 기준으로 삼는다. | 레이아웃·간격·자산·시각 위계는 Figma를 최대한 따르되, 제품 수치·필드·상태 순서가 충돌하면 `MVP_DEMO_CANONICAL.md`와 최종 유저플로우를 우선한다. |
| DEC-018 | 2026-08-18 | 승인 | 운영자 lifecycle command를 Next.js Route Handler로 실행하며 사용자 Bearer JWT로 RPC·RLS를 적용하고 service role은 멱등성 예약·캐시에만 사용한다. | 같은 키·정규화 요청은 기존 응답을 반환하고 다른 요청은 409로 차단한다. 환경설정 누락은 503으로 명시하며 실물 검수와 나머지 API는 아직 계약·RPC 경계다. |
| DEC-019 | 2026-08-18 | 승인 | 최신 사용자 지정 Figma 노드로 홈·공통 탐색·신청 내역 화면을 구체화한다. 하단 내비게이션은 `신청 내역`·`홈`·`마이페이지` 3개만 사용하고 `진행조회` 표기를 폐기한다. 홈은 `220:65`의 실제 영상을 재생하고 CTA는 `222:81`을 따른다. `/orders`는 신청 목록, `/orders/demo`는 목록에서 선택한 신청의 `261:63` 상세다. | `DEC-017`의 전체 시각 기준은 유지하되 해당 화면에 대한 기존 4탭 해석과 `/orders`→`/orders/demo` alias 해석은 이 결정이 대체한다. 신청 상세의 진행·배송 점은 인접 상태를 선으로 연결한다. |
| DEC-020 | 2026-08-18 | 승인 | 분석 입력 사진을 좌측면·우측면·하단·후면 4슬롯으로 바꾸고 네 장을 모두 필수로 한다. | 최신 사용자 촬영 요구가 DEC-012의 정면·측면·내부·각인 및 최소 3장 규칙을 대체한다. Storage 목적 enum은 유지하고 분석 배열 순서와 프런트 슬롯 메타로 방향을 관리한다. |
| DEC-021 | 2026-08-18 | 승인 | 분석 완료 후 접수 현황 재진입은 AI 결과로 건너뛰며, 추천은 Figma `228:782`의 트래블·지갑·파우치·키링 탭 구조를 따른다. 트래블의 여권지갑만 목업 상세로 진입하고 별도 고정 `3D 목업 미리보기` CTA는 제거한다. | 최초 접수의 짧은 분석 연출은 유지한다. 홈 상품 진단과 인증서 CTA는 같은 공통 ActionButton 컴포넌트를 사용한다. |
| DEC-022 | 2026-08-18 | 승인 | 분석 입력을 정면·후면·상단·하단·좌측면·우측면의 6면 사진과 일련번호 사진, 총 7개 필수 슬롯으로 확장한다. | 최신 사용자 요구가 DEC-020의 4슬롯 규칙을 대체한다. Storage 목적 enum은 늘리지 않고 프런트 슬롯 ID와 분석 자산 배열 순서로 구도를 관리한다. |
| DEC-023 | 2026-08-18 | 승인 | PC용 최소 통합 운영 콘솔을 `/operations` 목록과 `/operations/[applicationId]` 상세로 제공하고 단계별 관리자·장인 담당을 표시하며 준비된 신청을 다음 허용 단계로 넘길 수 있게 한다. | 데모에서는 중앙 Fixture와 현행 v2 상태 전이를 사용한다. `feature-backend`의 v1 API·주문 전 승인 흐름은 현 계약과 충돌하므로 직접 병합하지 않으며, 별도 장인 인증·배정·실데이터 연동은 후속 범위다. |
| DEC-024 | 2026-08-18 | 승인 | `origin/feature-backend`의 v1 Route Handler를 제거하고 23개 Route Handler 파일로 OpenAPI v2의 25개 operation을 구현한다. 고객·운영 UI는 당분간 Fixture를 유지하고 후속 작업에서 v2 API에 연결한다. 이벤트는 인증 사용자만 받고, `LIVE`는 공식 OpenAI JavaScript SDK의 Chat Completions Structured Outputs와 명시적 배포 opt-in·privacy notice·요청별 동의 증적을 사용하며 장애 시 Fixture로 폴백한다. | DB 적용은 신규·빈 v2 staging의 최종 bootstrap 또는 호환 기존 v2 데모 DB의 migration 001→004만 지원한다. v1 DB in-place 변환은 실제 백업 없이는 추정하지 않는다. 코드 구현은 실제 Supabase 프로젝트·migration·Auth/RLS/Storage/RPC나 OpenAI 호출 검증을 뜻하지 않는다. |
| DEC-026 | 2026-08-19 | 승인 | 배송 시작 lifecycle RPC는 `ON CONFLICT (application_id)` 대신 `ON CONFLICT ON CONSTRAINT mock_shipments_application_id_key`를 사용한다. 호환 기존 v2 DB에는 004 다음 `202608190005_shipment_conflict_hotfix.sql`을 적용한다. | PL/pgSQL `RETURNS TABLE` 출력 변수와 shipment 컬럼의 이름 충돌을 제거한다. HTTP 계약과 업무 데이터는 바뀌지 않는다. 005 rollback은 결함을 재도입하므로 앱까지 함께 되돌릴 때만 사용한다. |
| DEC-027 | 2026-08-19 | 승인 | 실물 검수 결과가 `CHANGE_REQUIRED`이면 고객의 승인·거절이 저장되기 전에는 운영자 lifecycle command로 `PRODUCTION_READY`에 진입할 수 없다. 운영 콘솔은 검수 결과를 `NO_CHANGE`·`CHANGE_REQUIRED`·`PRODUCTION_UNAVAILABLE` 중 선택해 저장하고, 고객 화면은 변경 전후 조건과 결정 UI·이력을 표시한다. | 고객 승인 전 제작 진행을 UI에만 의존하지 않도록 bootstrap과 기존 DB migration 006의 RPC·transition trigger에서 이중으로 차단한다. |
| DEC-025 | 2026-08-19 | 승인 | 고객·운영 브라우저 화면의 업무 데이터는 v2 API를 단일 데이터 소스로 사용한다. 신청 목록·상세·완료·마이페이지·보증서·분석·추천·주문·운영 콘솔은 인증 세션의 Supabase 응답을 렌더링하고, 빈 결과·인증 실패·API 오류를 중앙 Fixture로 대체하지 않는다. | `DEMO_FIXTURE`는 서버 분석 모드와 계약 예시로만 유지한다. 실제 CUSTOMER/OPERATOR 자격증명으로 전체 원격 여정을 검증하는 작업은 staging 게이트로 남긴다. |
| DEC-028 | 2026-08-20 | 승인 | 하단 내비게이션의 최상위 화면인 신청 내역과 마이페이지에는 뒤로가기를 표시하지 않는다. 하위 화면의 뒤로가기는 공통 `PageHeader`의 안전영역·44px 터치 좌표를 사용하며 몰입형 카메라도 같은 좌표를 따른다. 완료 신청 카드에는 해당 신청의 발급된 ESG 인증서 바로가기를 제공한다. | 최상위 탐색의 일관성을 높이고 화면마다 달랐던 뒤로가기 위치를 통일하며, 완료 사용자가 상세 화면을 거치지 않고 인증서에 접근할 수 있게 한다. |
| DEC-029 | 2026-08-21 | 승인 | 모든 페이지·3D·비동기 버튼 로딩은 공통 원형 인디케이터를 사용한다. `/` 시작 화면은 대표 이미지 준비 뒤 최소 1초 후 `/intro`로 자동 전환하며, 고객 로그인 직후에는 같은 화면에서 프로필·신청·진단을 초기 로딩한 뒤 `/home`으로 이동한다. 신청 내역과 마이페이지는 사용자별 브라우저 메모리 캐시를 즉시 표시하고 진입 시 조용히 재검증해 응답이 달라질 때만 갱신한다. | 반복 탭 이동의 불필요한 로딩을 제거한다. 고객 데이터는 브라우저 영구 저장소나 Next 공유 캐시에 복제하지 않고 로그아웃·401·만료·사용자 변경 때 즉시 폐기한다. 변경 감지용 revision/ETag가 없어 진입 시 백그라운드 조회와 표시 필드 fingerprint 비교를 사용한다. |
| DEC-030 | 2026-08-21 | 승인 | 7번째 시리얼번호 사진은 텍스트 자동 입력의 원천으로 사용한다. 제품 등록에는 Figma `228:651`을 따른 전용 `시리얼번호 촬영` 행과 11자리 영문·숫자 인증 UI를 제공하며, 촬영 완료 시 데모 값 `MK123456789`를 자동 입력한다. | `DEC-022`의 7장 분석 계약은 유지한다. 인증은 브라우저 데모 검증이며 API·DB의 선택 시리얼 계약은 변경하지 않는다. |
| DEC-031 | 2026-08-21 | 승인 | 분석 입력은 정면·후면·상단·하단·좌측면·우측면 6장만 필수로 한다. 시리얼 번호 사진은 선택이며 `시리얼 번호 촬영하여 입력`을 통해 데모 텍스트를 채우는 UI 보조 수단으로만 사용하고 분석 자산에는 포함하지 않는다. | 최신 사용자 요구가 `DEC-022`의 7장 필수 계약과 `DEC-030`의 시리얼 사진 필수 해석을 대체한다. `serialNumber` 텍스트의 API·DB 선택 계약과 11자리 UI 인증은 유지한다. |
| DEC-032 | 2026-08-21 | 승인 | 신규 `POST /analyses` 요청은 `serialNumber`를 필수로 받고, 앞뒤 공백을 제거한 값이 ASCII 영문자와 숫자를 각각 하나 이상 포함하는 정확히 11자리인지 API와 서비스에서 검증한다. 시리얼 번호 사진은 선택적인 자동 입력 보조 수단으로 유지한다. | 최신 사용자 요구가 `DEC-031`의 시리얼 텍스트 선택 계약만 대체한다. 기존 분석 행의 조회 호환성과 이력을 보존하기 위해 DB `analyses.serial_number`의 nullable 구조는 유지하며, 신규 쓰기 경계에서만 필수 조건을 적용한다. |
| DEC-033 | 2026-08-21 | 승인 | 시연 MVP에 분석 정보 기반 추천, 촬영 원본의 로컬 텍스처 추출·반복 타일 제작·GLB 결합을 필수 경로로 두고, 별도 동의와 배포 gate 뒤에 OpenAI 이미지 편집과 Meshy 2K 리텍스처를 선택 기능으로 제공한다. | 외부 장애·비용과 무관하게 로컬 목업을 완주하며, 유료 호출은 고객/분석/provider 멱등 예약과 고객·배포 일일 quota로 제한한다. 내부 `/api/demo/texture-preview`는 v2 제품 계약에 포함하지 않고 운영 승격 시 durable job·소유 asset storage·계약 변경을 별도로 수행한다. |

## 대체 관계

- `DEC-001`의 “기존 2주 계약 범위가 최신 기획보다 우선”하는 부분은 `DEC-010`으로 대체되었다.
- `DEC-004`의 live·fixture·hybrid 및 Mock 원칙은 유지하되 현재 골든 값은 `DEC-011`, `DEC-015`가 구체화한다.
- `DEC-008`의 신청 전 수동 검토 차단은 **2026-08-17 기준 superseded/historical**이며 `DEC-013`이 대체한다.
- `DEC-009`의 기술 스택은 유지한다. “scaffold 단계”라는 당시 상태 설명만 historical이며 현재 구현 여부는 코드와 검증 결과로 판단한다.
- `DEC-012`의 사진 구도와 최소 3장 규칙은 **2026-08-18 기준 `DEC-020`으로 대체**되었다. 파일 형식 JPG/PNG와 장당 10MB 제한은 유지한다.
- `DEC-020`의 4슬롯 촬영 규칙은 **2026-08-18 기준 `DEC-022`의 6면+일련번호 7슬롯 규칙으로 대체**되었다.
- `DEC-022`의 7장 필수 분석 계약과 `DEC-030`의 시리얼 사진 필수 해석은 **2026-08-21 기준 `DEC-031`의 6면 필수·시리얼 촬영 선택 규칙으로 대체**되었다.
- `DEC-031`의 시리얼 번호 사진 선택 규칙은 유지하되 `serialNumber` 텍스트 선택 계약은 **2026-08-21 기준 `DEC-032`의 신규 분석 접수 필수·11자리 검증 규칙으로 대체**되었다.
- `DEC-017`의 Figma 전체 시각 기준은 유지한다. 홈·하단 내비게이션·신청 내역의 구체적인 노드와 탐색 구조는 **2026-08-18 기준 `DEC-019`가 대체·구체화**한다.
- `DEC-004`의 사용자에게 보이는 분석 모드는 `DEMO_FIXTURE`, `SEEDED_ESTIMATE`, `LIVE`로 유지한다. `hybrid`는 별도 공개 enum이 아니라 `DEC-024`의 `LIVE` 내부 장애 폴백 동작으로 구체화한다.
- `DEC-018`의 “lifecycle command만 실행 경로”라는 당시 구현 상태는 historical이다. v2 서버 범위와 외부 연결 경계는 `DEC-024`가 대체한다.
- `DEC-024`의 “고객·운영 UI는 Fixture를 유지하고 후속 연결” 부분은 **2026-08-19 기준 `DEC-025`로 대체**되었다. v1 제거·v2 계약·LIVE 동의 원칙은 유지한다.
- 기존 행은 결정 당시 기록으로 보존한다. 현재 구현과 체크리스트에는 supersede 관계를 반영한 `DEC-010`~`DEC-033`을 적용한다.

## 새 결정 형식

```text
ID: DEC-NNN
날짜: YYYY-MM-DD
상태: 제안 | 승인 | 대체 | 폐기
결정:
배경:
선택지:
영향과 롤백:
승인자 또는 연결 이슈/PR:
대체한 결정:
```

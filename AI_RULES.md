# AI 작업 규칙

이 문서는 MCM RE:BORN 저장소에서 AI 에이전트가 지켜야 할 실행 규칙의 기준이다. 사람의 명시적 지시와 승인된 이슈 범위 안에서만 작업한다.

## 1. 기준과 우선순위

- 현재 제품 기준은 `develop` 브랜치의 2주 해커톤 웹 MVP다.
- 로컬 기획의 확장 기능은 이후 로드맵으로 분리하며 승인 없이 MVP에 포함하지 않는다.
- 제품 범위는 `docs/PROJECT_CONTEXT.md`와 `docs/PRD.md`, 사용자 흐름은 `docs/USER_FLOW.md`를 따른다.
- API의 기계 판독 기준은 `openapi.yaml`이다. 구현 가이드는 `MCM_REBORN_API_GUIDE.md`, Mock은 `mock-data.json`, DB 구조와 RLS는 `supabase-schema.sql`을 따른다.
- 의사결정 이력은 `docs/DECISIONS.md`에 기록한다.
- 문서끼리 또는 문서와 구현이 충돌하면 변경하지 말고 차이, MVP 영향, 선택지를 보고해 승인을 받는다.
- Next.js 애플리케이션은 `mcm-reborn/`에 있다. 앱 파일을 다룰 때는 루트 `AGENTS.md`와 이 문서를 적용한 뒤 `mcm-reborn/AGENTS.md`를 추가로 읽으며, 중첩 지침을 루트 승인·보안·Git 규칙의 대체물로 해석하지 않는다.

## 2. 작업 전 확인

다음 항목이 준비되어야 구현을 시작할 수 있다.

- 이슈와 담당자
- 해결할 사용자 문제와 우선순위(P0~P3)
- 검증 가능한 완료 기준
- 제외 범위와 의존성
- UI 상태 또는 API 요청·응답·오류 계약
- 필요한 사람의 사전 승인

누락된 정보가 구현 결과를 크게 바꾸면 질문한다. 영향을 주지 않는 세부사항은 기존 패턴을 근거로 최소한의 가정을 명시하고 진행한다.

## 3. 범위 제어

- 먼저 수정 예정 파일과 영향 범위를 확인한다.
- 한 이슈에 필요한 최소 변경만 한다.
- 관련 없는 포맷 변경, 이름 변경, 리팩터링을 섞지 않는다.
- 기존 코드, 타입, 컴포넌트, 라이브러리를 우선 재사용한다.
- 기억에 의존해 라이브러리 API를 추측하지 말고 저장소의 실제 타입·버전과 공식 문서를 확인한다.
- 승인 없는 패키지·프레임워크 추가, 전체 재작성, 계약 변경, 테스트 삭제를 금지한다.
- 하루를 넘기는 작업은 독립적으로 검증하고 병합할 수 있는 단위로 분리한다.

## 4. Git과 리뷰

- 최신 `develop`에서 시작한다.
- 브랜치는 `feature-*`, `bugfix-*`, `hotfix-*` 형식을 사용한다.
- 커밋 유형은 `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`를 사용한다.
- 이슈 하나당 PR 하나를 원칙으로 한다.
- `main`과 `develop`에 직접 push하지 않는다.
- PR 작성자는 자신의 PR을 승인하지 않는다. 병합은 사람의 승인과 필수 검사가 끝난 뒤 팀의 권한 정책을 따른다.
- 사람의 승인과 필수 CI 검사가 모두 끝나기 전에는 병합 준비 완료로 판단하지 않는다.
- 브랜치 생성, 커밋, push, PR 생성과 병합은 사용자나 연결된 이슈가 명시적으로 요청한 범위에서만 수행한다.

## 5. API·데이터 계약

다음 변경은 단순 구현이 아니라 계약 변경이다.

- 엔드포인트, HTTP 메서드, 인증·권한
- 요청·응답 필드, 오류 형식, 상태값
- OpenAPI schema 또는 example
- Mock 데이터의 소비자 계약
- DB table, column, constraint, RLS 정책

계약 변경은 별도 이슈와 프론트엔드·백엔드 양쪽 승인이 필요하다. 승인이 없으면 구현 파일을 바꾸지 말고 변경 제안과 마이그레이션 영향을 작성한다. 승인 후에는 계약과 예시 → Mock·타입 → 서버·DB → 클라이언트 → 통합 테스트·문서 순서로 갱신한다.

## 6. 보안과 데이터

- API 키, 토큰, 쿠키, 비밀번호, 서비스 역할 키를 저장소에 기록하지 않는다.
- 로그와 오류 메시지에 인증정보, 개인 정보, 원본 업로드 URL을 노출하지 않는다.
- 샘플 데이터는 가상임을 명확히 하고 실제 고객·운영 데이터를 사용하지 않는다.
- 인증과 RLS를 우회하거나 권한 검사를 Mock으로 대체하지 않는다.
- 결제, 물류, ESG 수치, 보증서 등 MVP Mock 기능을 실제 기능처럼 표현하지 않는다.

## 7. 검증 기준

변경 유형에 맞게 다음을 검증한다.

- 정상 흐름
- 오류와 외부 서비스 실패 흐름
- 빈 데이터와 로딩 상태
- 고객·운영자 권한 경계
- lint, 타입 검사, 단위·통합 테스트, 빌드
- OpenAPI·Mock·DB·문서 간 정합성
- 데모 핵심 흐름과 폴백

검사 실패를 숨기거나 테스트를 약화하지 않는다. 실행할 수 없는 검사는 이유, 수동 확인 방법, 남은 위험을 결과에 기록한다.

`mcm-reborn/` 변경은 저장소 루트에서 다음 명령으로 검증한다.

```text
npm --prefix mcm-reborn run lint
npm --prefix mcm-reborn run typecheck
npm --prefix mcm-reborn run build
```

재현 가능한 의존성 설치가 필요하면 먼저 `npm --prefix mcm-reborn ci`를 실행한다. 현재 `mcm-reborn/package.json`에는 자동 테스트 스크립트가 없으므로 테스트 성공을 주장하지 말고 검증 공백으로 기록한다.

협업 문서, GitHub 템플릿 또는 저장소 스킬을 바꾼 경우 `python -X utf8 scripts/validate_collaboration.py`도 실행한다.

## 8. AI 사용 공개

PR 설명에 아래 항목을 포함한다.

- 사용한 AI 도구 또는 에이전트
- AI가 수행한 작업 범위
- 사람이 직접 검토한 파일과 동작
- 실행한 검사와 결과
- 미확인 항목과 위험

AI 결과를 검토 없이 병합 가능한 것으로 표시하지 않는다.

## 9. 완료 보고

작업 종료 시 변경 파일, 핵심 결정, 검증 결과, 미해결 위험, 후속 작업을 간결하게 보고한다. 계약 또는 로드맵 판단을 위해 받은 사용자 승인은 `docs/DECISIONS.md`나 연결된 이슈·PR에 남긴다.
